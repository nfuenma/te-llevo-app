# Base de datos (requerida por prisma.config.ts / schema; el CLI carga .env vía dotenv en prisma.config.ts)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/te_llevo"

# Cloudinary (subida de imágenes optimizadas desde el admin)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
